server {
    listen 80;
    listen [::]:80;
    server_name thescoremachine.com www.thescoremachine.com admin.thescoremachine.com super-admin.thescoremachine.com affiliate.thescoremachine.com support.thescoremachine.com funding-manager.thescoremachine.com member.thescoremachine.com printing-team.thescoremachine.com ref.thescoremachine.com onboarding.thescoremachine.com api.thescoremachine.com;

    # Nginx defaults to a 1MB request body limit if this is omitted.
    client_max_body_size 100M;

    # Temporary HTTP-only bootstrap config.
    # Use this first so nginx can start before certificates exist.

    location /socket.io/ {
        proxy_pass http://127.0.0.1:3001/socket.io/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location / {
        proxy_pass http://127.0.0.1:3001/;
        proxy_http_version 1.1;
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
        send_timeout 120s;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "keep-alive";
    }
}